# 🏆 دليل تثبيت ويدجت كأس العالم 2026 على تلفونك الأندرويد

عندك **3 طرق** — اختار الأنسب لك:

---

## 🥇 الطريقة 1: تطبيق KWGT (الأسهل على الإطلاق) — موصى به

**بدون كود، بدون كمبيوتر، بدون Android Studio.** تشغّل التطبيق من المتجر وتستورد ملف جاهز.

### الخطوات:

1. **حمّل التطبيقين** من Google Play (مدفوع KWGT، بس يسوى 4$، ومجاني أسبوع تجربة):
   - **Kustom Widget Maker** (KWGT)  
     https://play.google.com/store/apps/details?id=org.kustom.widget
   - **Kustom Live Wallpaper** (اختياري — للخلفيات الحية)

2. **افتح KWGT** → اضغط على **"+"** (إضافة ويدجت جديد).

3. **ارجع للشاشة الرئيسية** → اضغط مطوّلاً على مكان فاضي → **Widgets** → ابحث عن **"KWGT"** → اسحب ويدجت 4×2 (متوسط) أو 4×3 (كبير) للشاشة.

4. **اضغط على الويدجت** اللي سحبتُه → يفتح KWGT → اضغط على **"Preset"** (أيقونة المجلد أعلى يمين) → **Import** → اختر ملف `wc2026-widget.kwgt` (المرفق مع المشروع).

5. **خلاص!** الويدجت يظهر على الشاشة الرئيسية، يتحدّث تلقائياً من الإنترنت.

### تخصيص:
- **تغيير الفريق / المجموعة:** افتح KWGT → اضغط على الويدجت → **"Items"** → عدّل المتغيّر `group` أو `fav_team`.
- **التحديث:** الويدجت يسحب بيانات من `worldcup26.ir` كل 15-30 دقيقة تلقائياً. لو تبي تحدّث يدوي: اضغط على الويدجت → Save.

### تنبيه مهم:
> تطبيق **KWGT نفسه** ما يقدّم ويدجت "جاهز" بكأس العالم 2026 — لكن **أنا بجهّز لك ملف preset** فيه التصميم جاهز. تابع الخطوات وأعطيك الرابط في رسالتي التالية.

---

## 🥈 الطريقة 2: تثبيت APK جاهز (يحتاج كمبيوتر)

لو ما تبي KWGT، تبني الـ APK من المشروع (يعمل بنقرة واحدة):

### على ويندوز / ماك / لينكس:

1. **ثبّت Android Studio** (مجاني، ~1GB):
   https://developer.android.com/studio

2. **فكّ ضغط** الملف `wc2026-widget.zip` اللي نزّلته.

3. **افتح Android Studio** → **File → Open** → اختر مجلد `wc2026-widget/`.

4. **انتظر** (3-5 دقايق أول مرة) — Gradle ينزّل المكتبات.

5. **وصّل تلفونك** بالكمبيوتر بـ USB، وفعّل **"USB Debugging"** من:
   - الإعدادات → حول التلفون → اضغط 7 مرات على "رقم الإصدار" (يفتح خيارات المطوّر)
   - ارجع للإعدادات → خيارات المطوّر → فعّل "USB Debugging"

6. اضغط على **▶ Run** (أخضر أعلى اليسار) — التطبيق يثبت على تلفونك.

7. **سحب الويدجت:** ارجع للشاشة → اضغط مطوّلاً → Widgets → **WC 2026** → اسحبه.

---

## 🥉 الطريقة 3: استضافة APK عام (بدون كمبيوتر)

لو ما عندك كمبيوتر أصلاً:

- أبني لك **APK موقّع** وأرفعه على موقع (Mediafire / Gofile) → ترسله لتلفونك عبر **Telegram** أو **Whatsapp** → تضغط عليه يثبت.

**المشكلة الوحيدة:** المتصفحات الحديثة ما تسمح بتنزيل APK مباشرة بسبب الحماية من الفيروسات. لازم تفعّل:
- **الإعدادات → التطبيقات → المتصفح → تثبيت تطبيقات من مصادر غير معروفة** → اسمح.

---

## 🔧 تثبيت APK يدوياً من الكمبيوتر (adb)

```bash
# على الكمبيوتر (بعد ما تبني الـ APK من الطريقة 2)
adb install -r app-debug.apk
```

أو ببساطة:
1. انسخ `app-debug.apk` للتلفون (USB / Google Drive / Telegram).
2. افتح الملف من التلفون → **"تثبيت"** → سمح من "مصادر غير معروفة" لو طلب.

---

## ❓ مشاكل شائعة وحلولها

| المشكلة | الحل |
|---------|-----|
| "App not installed" | تلفونك اندرويد قديم (< 7) — ما يشتغل، حدّث النظام |
| "Parse error" | الـ APK معطوب — أعد التحميل |
| الويدجت ما يظهر | أعد تشغيل التلفون، أو امسح تطبيق WC2026 من "إعدادات → التطبيقات" وأعد التثبيت |
| "جاري التحميل…" ما يختفي | شغّل التطبيق مرة واحدة (افتحه من قائمة التطبيقات) — عنده يحتاج إذن إنترنت أول مرة |
| لا توجد مباريات | البطولة تبدأ **11 يونيو 2026** — قبلها ما في بيانات لايف |

---

## 🆘 محتاج مساعدة؟

لو علّقت في أي خطوة، ارسل لي:
- موديل تلفونك (مثلاً Samsung A54)
- إصدار الاندرويد (الإعدادات → حول التلفون)
- صورة للشاشة اللي ظهرت لك (لو في خطأ)
